# 🎨 MLOps Pipeline Architecture

This document provides a visual overview of the complete MLOps pipeline.

---

## 📊 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         DATA SOURCES (Django DB)                     │
│   JobRole • Skill • MarketTrend • EconomicReport • HRInvestment     │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
                                ▼
                ┌───────────────────────────────┐
                │   DATASET EXPORT              │
                │   manage.py export_future_    │
                │   skills_dataset              │
                └───────────────┬───────────────┘
                                │
                                ▼
                ┌───────────────────────────────┐
                │   CSV STORAGE                 │
                │   ml/future_skills_           │
                │   dataset.csv                 │
                └───────────────┬───────────────┘
                                │
                                ▼
                ┌───────────────────────────────┐
                │   MODEL TRAINING              │
                │   train_future_skills_        │
                │   model.py --version vX       │
                └───────┬───────────────────────┘
                        │
        ┌───────────────┴────────────────┐
        ▼                                ▼
┌──────────────────┐          ┌──────────────────┐
│   MODEL PKL      │          │   METADATA JSON  │
│   future_skills_ │          │   - Metrics      │
│   model_vX.pkl   │          │   - Features     │
│   (Pipeline)     │          │   - Hyperparams  │
└────────┬─────────┘          └────────┬─────────┘
         │                              │
         └──────────┬───────────────────┘
                    ▼
         ┌─────────────────────┐
         │   REGISTRY UPDATE   │
         │   MODEL_REGISTRY.md │
         └──────────┬──────────┘
                    │
                    ▼
         ┌─────────────────────┐
         │   DEPLOYMENT        │
         │   Update settings.py│
         │   - MODEL_PATH      │
         │   - MODEL_VERSION   │
         └──────────┬──────────┘
                    │
                    ▼
         ┌─────────────────────┐
         │   PRODUCTION        │
         │   Django API        │
         │   Predictions       │
         └──────────┬──────────┘
                    │
                    ▼
         ┌─────────────────────┐
         │   MONITORING        │
         │   predictions_      │
         │   monitoring.jsonl  │
         └──────────┬──────────┘
                    │
                    ▼
         ┌─────────────────────┐
         │   DRIFT ANALYSIS    │
         │   (Future)          │
         │   analyze_drift.py  │
         └─────────────────────┘
```

---

## 🔄 Retraining Workflow

```
┌────────────────────────────────────────────────────────────────┐
│                   TRIGGER RETRAINING                           │
│   • Scheduled (weekly/monthly)                                 │
│   • Manual (make retrain-future-skills)                       │
│   • Drift detected (automatic alert)                          │
└────────────────────────┬───────────────────────────────────────┘
                         │
                         ▼
         ┌───────────────────────────────┐
         │   1. EXPORT FRESH DATASET     │
         │   python manage.py export_... │
         └───────────────┬───────────────┘
                         │
                         ▼
         ┌───────────────────────────────┐
         │   2. TRAIN NEW VERSION        │
         │   train_model.py --version vX │
         └───────────────┬───────────────┘
                         │
                         ▼
         ┌───────────────────────────────┐
         │   3. GENERATE METADATA        │
         │   Auto: future_skills_vX.json │
         └───────────────┬───────────────┘
                         │
                         ▼
         ┌───────────────────────────────┐
         │   4. UPDATE REGISTRY          │
         │   Auto: MODEL_REGISTRY.md     │
         └───────────────┬───────────────┘
                         │
                         ▼
         ┌───────────────────────────────┐
         │   5. EVALUATE METRICS         │
         │   Compare with previous       │
         └───────────────┬───────────────┘
                         │
                 ┌───────┴────────┐
                 │                │
        ┌────────▼─────┐   ┌─────▼──────────┐
        │ Metrics OK?  │   │ Metrics Worse? │
        │   ✅         │   │    ❌          │
        └────────┬─────┘   └─────┬──────────┘
                 │               │
                 │               ▼
                 │      ┌──────────────────┐
                 │      │ INVESTIGATE      │
                 │      │ - Check data     │
                 │      │ - Adjust params  │
                 │      │ - Keep old model │
                 │      └──────────────────┘
                 │
                 ▼
         ┌───────────────────────────────┐
         │   6. UPDATE SETTINGS          │
         │   Auto or Manual              │
         └───────────────┬───────────────┘
                         │
                         ▼
         ┌───────────────────────────────┐
         │   7. DEPLOY TO PRODUCTION     │
         │   Restart Django server       │
         └───────────────┬───────────────┘
                         │
                         ▼
         ┌───────────────────────────────┐
         │   8. MONITOR                  │
         │   Log predictions for drift   │
         └───────────────────────────────┘
```

---

## 🗂️ File Structure

```
smarthr360_m3_future_skills/
│
├── ml/                                    # ML Directory
│   ├── train_future_skills_model.py      # Training script (versioned)
│   ├── retrain_model.py                  # ✨ Retraining orchestration
│   ├── evaluate_future_skills_models.py  # Evaluation script
│   │
│   ├── future_skills_model_v1.pkl        # Model binary v1
│   ├── future_skills_model_v1.json       # ✨ Metadata v1
│   ├── future_skills_model_v2.pkl        # Model binary v2
│   ├── future_skills_model_v2.json       # ✨ Metadata v2
│   │
│   ├── future_skills_dataset.csv         # Training dataset
│   │
│   ├── MODEL_REGISTRY.md                 # ✨ Version registry
│   ├── MLOPS_GUIDE.md                    # ✨ Complete guide
│   ├── QUICK_REFERENCE.md                # ✨ Quick commands
│   └── README.md                         # General ML docs
│
├── logs/
│   ├── future_skills.log                 # General logs
│   └── predictions_monitoring.jsonl      # ✨ Prediction logs
│
├── config/
│   └── settings.py                       # 🔧 ML config
│
├── future_skills/
│   └── services/
│       └── prediction_engine.py          # 🔧 Monitoring added
│
├── Makefile                               # ✨ Simplified commands
│
└── docs/
    └── LT2_COMPLETION_SUMMARY.md         # ✨ Summary
```

**Legend:**

- ✨ New files created for LT-2
- 🔧 Modified files
- Regular files (existing)

---

## 🎯 Data Flow

```
┌─────────┐     ┌─────────┐     ┌──────────┐     ┌──────────┐
│ JobRole │────▶│  Skill  │────▶│  Trend   │────▶│ Features │
└─────────┘     └─────────┘     └──────────┘     └──────────┘
                                                        │
                                                        ▼
                                              ┌──────────────────┐
                                              │  ML Pipeline     │
                                              │  1. OneHot       │
                                              │  2. Scaler       │
                                              │  3. RandomForest │
                                              └────────┬─────────┘
                                                       │
                                            ┌──────────┴──────────┐
                                            │                     │
                                            ▼                     ▼
                                    ┌─────────────┐    ┌──────────────┐
                                    │  Level      │    │  Score       │
                                    │  LOW/MED/   │    │  0-100       │
                                    │  HIGH       │    │              │
                                    └──────┬──────┘    └──────┬───────┘
                                           │                  │
                                           └────────┬─────────┘
                                                    ▼
                                         ┌────────────────────┐
                                         │ FutureSkill        │
                                         │ Prediction         │
                                         │ (DB Record)        │
                                         └────────────────────┘
```

---

## 🔐 Permissions Matrix

```
┌──────────────────┬───────────┬──────────┬──────────┬────────────┐
│ Action           │ Data Sci  │ DRH Mgr  │ DevOps   │ Developer  │
├──────────────────┼───────────┼──────────┼──────────┼────────────┤
│ Export Dataset   │    ✅     │    ✅    │    ✅    │     ❌     │
│ Train Model      │    ✅     │    ❌    │    ✅    │     ❌     │
│ View Metrics     │    ✅     │    ✅    │    ✅    │     ✅     │
│ Approve Deploy   │    ❌     │    ✅    │    ❌    │     ❌     │
│ Deploy Model     │    ❌     │    ❌    │    ✅    │     ❌     │
│ View Logs        │    ✅     │    ✅    │    ✅    │     ✅     │
│ Analyze Drift    │    ✅     │    ❌    │    ✅    │     ❌     │
└──────────────────┴───────────┴──────────┴──────────┴────────────┘
```

---

## 📊 Monitoring Dashboard (Future)

```
┌─────────────────────────────────────────────────────────────┐
│               FUTURE SKILLS ML DASHBOARD                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │ Model v2     │  │ Accuracy     │  │ Predictions  │    │
│  │ Active       │  │ 85.4%        │  │ Today: 1,234 │    │
│  └──────────────┘  └──────────────┘  └──────────────┘    │
│                                                             │
│  📈 Accuracy Trend (Last 30 Days)                          │
│  ┌──────────────────────────────────────────────┐         │
│  │ 90% ┤                                         │         │
│  │ 85% ┤══════════════════════════════          │         │
│  │ 80% ┤                                         │         │
│  │ 75% ┤                                         │         │
│  └──────────────────────────────────────────────┘         │
│                                                             │
│  🔥 Feature Distribution Drift                             │
│  ┌──────────────────────────────────────────────┐         │
│  │ trend_score:      [====|====]  ✅ Normal     │         │
│  │ internal_usage:   [====|=====] ✅ Normal     │         │
│  │ training_req:     [====|========] ⚠️ Drift   │         │
│  └──────────────────────────────────────────────┘         │
│                                                             │
│  📊 Per-Class Performance                                  │
│  ┌──────────────────────────────────────────────┐         │
│  │ LOW:     85% │███████████░░░░│               │         │
│  │ MEDIUM:  83% │██████████░░░░░│               │         │
│  │ HIGH:    88% │████████████░░░│               │         │
│  └──────────────────────────────────────────────┘         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 Model Extensibility Architecture

### Design Principles

The ML pipeline is architected to be **model-agnostic** through:

1. **Interface Contract**: All models must implement sklearn-compatible `fit()` and `predict()` methods
2. **Pipeline Composition**: `ColumnTransformer` → `Estimator` → Output
3. **Serialization Standard**: Models saved as `.pkl` with JSON metadata
4. **API Abstraction**: Business logic decoupled from model internals

### Supported Model Types

```
┌────────────────────────────────────────────────────────┐
│                  SKLEARN-COMPATIBLE                     │
│                     ESTIMATORS                          │
├────────────────────────────────────────────────────────┤
│                                                         │
│  Tree-Based Models        Linear Models                │
│  ┌─────────────────┐     ┌──────────────────┐        │
│  │ RandomForest ✅  │     │ LogisticReg ✅   │        │
│  │ XGBoost         │     │ Ridge/Lasso      │        │
│  │ LightGBM        │     │ ElasticNet       │        │
│  │ GradientBoost   │     └──────────────────┘        │
│  └─────────────────┘                                  │
│                                                         │
│  Neural Networks          Ensemble Methods             │
│  ┌─────────────────┐     ┌──────────────────┐        │
│  │ MLPClassifier   │     │ VotingClassifier │        │
│  │ Keras (wrapper) │     │ StackingClass    │        │
│  └─────────────────┘     └──────────────────┘        │
│                                                         │
└────────────────────────────────────────────────────────┘
                           │
                           ▼
            ┌──────────────────────────┐
            │   PREDICTION INTERFACE   │
            │   predict(X) → y         │
            │   classes_ → [L,M,H]     │
            └──────────────────────────┘
```

### Model Selection Workflow

```
┌─────────────────────────────────────────────────────────┐
│  1. EXPERIMENTATION PHASE                               │
│  python ml/experiment_future_skills_models.py          │
│                                                         │
│  Tests multiple algorithms:                            │
│  • RandomForest (baseline)                             │
│  • LogisticRegression                                   │
│  • XGBoost / LightGBM (optional)                       │
│                                                         │
│  Outputs: MODEL_COMPARISON.md                          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  2. SELECTION CRITERIA                                  │
│  ┌─────────────────────────────────────────────────┐  │
│  │ ✅ F1-Score (Primary)         Target: >0.95     │  │
│  │ ✅ CV Stability               Std < 0.01        │  │
│  │ ✅ Interpretability           Feature import.   │  │
│  │ ✅ Training Time              < 1 min           │  │
│  │ ✅ Dependencies               Minimal deps      │  │
│  └─────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  3. CURRENT SELECTION: RandomForest                     │
│                                                         │
│  Rationale:                                             │
│  • F1: 0.986 (excellent)                               │
│  • CV: 0.9929 ± 0.0087 (stable)                        │
│  • Feature importance available                         │
│  • Pure sklearn (no extra deps)                        │
│  • Training: 0.19s (fast)                              │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│  4. ALTERNATIVE: LogisticRegression                     │
│                                                         │
│  Could replace if:                                      │
│  • Need faster inference (0.02s vs 0.19s)              │
│  • Simpler model preferred                              │
│  • F1: 0.9862 (slightly better)                       │
│  • Trade-off: Less interpretable                       │
└─────────────────────────────────────────────────────────┘
```

### Model Replacement Process

```bash
# 1. Update model in training script
vim ml/train_future_skills_model.py
# Change: clf = RandomForestClassifier(...)
# To:     clf = YourNewModel(...)

# 2. Retrain with new model
python ml/train_future_skills_model.py --version v2

# 3. Compare performance
python ml/experiment_future_skills_models.py

# 4. Update registry
vim ml/MODEL_REGISTRY.md

# 5. Deploy (no API changes needed)
cp ml/future_skills_model_v2.pkl ml/future_skills_model.pkl

# 6. Restart application
# Model automatically reloaded on next prediction
```

**Key Point**: The entire swap requires **zero API changes** because the interface `(level, score)` remains constant.

---

**Created**: 2025-11-27  
**Updated**: 2025-11-27 (Added model extensibility section)  
**Purpose**: Visual reference for MLOps pipeline architecture
